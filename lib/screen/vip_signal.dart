import 'package:flutter/material.dart';


class VipSignalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Container(
          child: Text('Vip Screen'),
        ),
      ),
    );
  }
}